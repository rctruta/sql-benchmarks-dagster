meta:
  experiment_id: "RESET"
  description: "Resetting stuck config"

dataset:
  source: "sql_benchmarks.plugins.data_sources.declarative_gen"
  tables: 
    dummy:
      rows: 1
      columns:
        - name: id
          provider: sequence

execution:
  engines: [duckdb]
  matrix:
    size: [default]
    
definitions:
  rows:
    default: 1
