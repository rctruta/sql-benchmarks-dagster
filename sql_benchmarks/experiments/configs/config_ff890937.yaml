meta:
  experiment_id: ff890937
  description: Clean architecture test
dataset:
  source: sql_benchmarks.plugins.data_sources.declarative_gen
  tables:
    orders:
      rows: 100000
      columns:
      - name: id
        provider: sequence
execution:
  test_suite: selectivity
  replication: 3
  engines:
  - postgres
  - duckdb
  matrix:
    rows:
    - 100000
    disk_type:
    - ssd
    - hdd
