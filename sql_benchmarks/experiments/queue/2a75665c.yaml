dataset:
  source: sql_benchmarks.plugins.data_sources.declarative_gen
  tables:
    analytical_data:
      rows: 100000
execution:
  test_suite: analytical_wall
  engines:
  - duckdb
  - postgres
  replication: 3
