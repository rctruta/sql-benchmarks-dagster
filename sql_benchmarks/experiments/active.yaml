dataset:
  source: sql_benchmarks.plugins.data_sources.declarative_gen
  tables:
    skewed_data:
      columns:
      - name: id
        provider: sequence
      rows: 100
execution:
  engines:
  - postgres
  - duckdb
  matrix:
    disk_type:
    - ssd
    - hdd
    rows:
    - 100
  replication: 3
  test_suite: selectivity
meta:
  description: Clean architecture test
  experiment_id: v7_refactor
